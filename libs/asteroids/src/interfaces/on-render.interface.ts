/**
 * Interface that represents some class that has a `onRender` method that is
 * called when the entity is rendered on screen.
 */
export interface IOnRender {
  /**
   * Method that is called when the entity is rendered on screen.
   */
  onRender(): void
}
